async function loadMessages(){const res=await fetch('/api/messages');const data=await res.json();const root=document.getElementById('messages');root.innerHTML='';for(const item of data.items||[]){const el=document.createElement('article');el.className='msg';const t=new Date((item.created_at||0)*1000).toLocaleString('zh-TW',{hour12:false});el.innerHTML=`<div class="meta">${item.nickname} · ${t}</div><div>${escapeHtml(item.body).replace(/\n/g,'<br>')}</div>`;root.appendChild(el)}root.scrollTop=root.scrollHeight}function escapeHtml(v){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}document.getElementById('refreshBtn')?.addEventListener('click',loadMessages);document.getElementById('composer')?.addEventListener('submit',async(e)=>{e.preventDefault();const body=document.getElementById('body');const res=await fetch('/api/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({body:body.value})});const data=await res.json();if(!res.ok){alert(data.error||'送出失敗');return}body.value='';loadMessages()});loadMessages();setInterval(loadMessages,5000);