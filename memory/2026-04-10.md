
- 最後又處理了兩個前端技術債：一是 weather 卡在接入統一收折系統後，舊的 `weather-panel-collapsed` 類別還在干擾，導致展開後看不到資料；我已改成只服從新的統一收折系統。二是 `ui.js` 和 `guestbook.js` 原本各自 new 一個 Supabase client，造成瀏覽器出現 multiple GoTrueClient 警告；現在已抽成共用的 `docs/supabase-client.js` 單例。提交為 `f0a9357`。
- 晚上又補了一輪 dashboard 偏好同步與川普翻譯優化：`dashboard_preferences` 現在會把 `collapsed_cards` 一起讀寫到 Supabase，並清掉 weather 卡片 HTML 上殘留的舊收折 class；另外把 `scripts/build_dashboard_data.py` 裡的川普繁中翻譯後處理規則加強，讓公開版文案更接近台灣常用語感、少一點直譯味。相關提交為 `95308f1` 與後續翻譯優化提交。
