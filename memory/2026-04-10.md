# 2026-04-10

- 早上確認 LINE 收不到訊息的根因是 ngrok webhook endpoint 離線，外部 URL 回 `ERR_NGROK_3200`，不是 OpenClaw 本機 `/line/webhook` 路由故障。
- 經 Koxuan 同意後重新啟動 ngrok，取得新的公開 URL `https://6541-60-251-41-139.ngrok-free.app`，並實測 `POST /line/webhook` 可回 200 `{"status":"ok"}`。
- Koxuan 擔心 cron 長期默默消耗 token，因此要求調降兩個高頻 job 的排程。
- 經 Koxuan 同意，已將 `Trump Truth important alerts` 從每 10 分鐘改為每 30 分鐘，將 `US market monitor` 從每 15 分鐘改為每 30 分鐘。
- 之後依 Koxuan 要求進一步節流：`market_monitor.py` 現在只有出現明顯異常波動才會輸出通知，否則回 `NO_REPLY`。
- `Trump Truth important alerts` 也新增去重邏輯，若 3 小時內屬於同類摘要的重複重大貼文，會直接回 `NO_REPLY`。
- 另已把 `Trump Truth important alerts` 限縮為台灣時間 08:00-23:30 每半小時執行，`Canon RF45 stock watch` 限縮為台灣時間 09:00-22:00 整點執行，以降低離峰時段 token 消耗。
- 下午開始針對 doggo dashboard 做 50 分到 80 分的第一波產品化升級，方向是強化主敘事、把狗狗變成資訊導覽員、統一主卡 HUD 語言，而不是繼續堆零碎功能。
- 第一波已實作：新增 hero HUD 區塊、把三張主資訊卡改成統一 intel panel 結構、加入 badge / kicker / focus 狀態、以及 hover 卡片時由狗狗主動導覽對話。
